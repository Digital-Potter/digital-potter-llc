/**
 * Mega-menu placeholder copy. The CMS-driven mega menu (configured via
 * `isMegaMenu` + `megaMenuColumns` on a navigation item) supplies the real
 * eyebrow/subtitle and column items. These constants are the only piece of
 * hardcoded content the front-end keeps — used as per-FIELD fallbacks when
 * a CMS-configured trigger leaves `megaMenuTitle` / `megaMenuSubtitle`
 * empty.
 */
export const SERVICES_MEGAMENU_HEADER = {
	eyebrow: 'Our Services',
	subtitle: 'Custom websites, apps, and a CMS your team owns.',
};
